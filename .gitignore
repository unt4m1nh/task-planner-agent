# SQLite databases (local, regenerable)
data/*.db
data/*.db-shm
data/*.db-wal

# LangGraph API
.langgraph_api

# Local Claude Code settings
.claude/settings.local.json

# Scratch planning notes
plan/
